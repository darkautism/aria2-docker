version: '3'

services:
  aria2:
    image: ghcr.io/darkautism/aria2-docker:latest
    container_name: aria2
    volumes:
      - ./config:/config
      - ./downloads:/downloads
    environment:
      - RPC_SECRET=your-secret-token
    ports:
      - "6800:6800"
      - "6881-6999:6881-6999"
    restart: unless-stopped

  ariang:
    image: ghcr.io/darkautism/aria2-docker-ariang:latest
    container_name: ariang
    environment:
      - ARIA2_HOST=aria2
      - ARIA2_PORT=6800
      - ARIA2_SECRET=your-secret-token
    ports:
      - "8080:8080"
    restart: unless-stopped